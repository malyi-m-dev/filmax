import SwiftUI

// Тёмная кино-тема Filmax (акцент #B4305A — как на Android/дизайн-системе).
enum Theme {
    static let accent = Color(red: 0xB4 / 255, green: 0x30 / 255, blue: 0x5A / 255)
    static let accentSoft = Color(red: 0x6B / 255, green: 0x4B / 255, blue: 0x8F / 255)
    static let background = Color.black
    static let surface = Color(white: 0.10)
    static let onSurfaceVariant = Color(white: 0.65)
}
